<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ValidateSMSTokenActivity">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center_vertical">

        <LinearLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="8dp"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        android:paddingVertical="30dp">


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Código de validação" />

        <EditText
            android:id="@+id/smsTokenPhone"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="5dp"
            android:background="@drawable/mycascais_edittext"
            android:cursorVisible="true"
            android:drawableLeft="@drawable/ic_phone"
            android:drawablePadding="11dp"
            android:imeOptions="actionNext"
            android:inputType="phone"
            android:paddingHorizontal="10dp"
            android:paddingVertical="10dp" />

        <Button
            android:id="@+id/accountValidateSmsToken"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"

            android:paddingVertical="15dp"
            android:text="@string/mycascais_confirm"
            android:textColor="@color/colorWhite"
            android:textSize="18dp" />

    </LinearLayout>

</FrameLayout>
</FrameLayout>